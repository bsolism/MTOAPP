import React from "react";
import { Divider } from "@mui/material";

export default function FieldDivider() {
  return (
    <Divider
      style={{
        width: "100%",
        marginTop: "10px",
        marginBottom: "10px",
      }}
    />
  );
}
